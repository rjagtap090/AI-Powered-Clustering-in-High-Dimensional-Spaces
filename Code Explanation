
Data Loading and Cleaning:

Load the dataset.
Clean the data by removing any missing values.
Feature Selection:

Select the relevant features for clustering.
Dimensionality Reduction:

Apply PCA to reduce the dataset to 2 principal components.
Apply t-SNE to reduce the dataset to 2 dimensions.
Clustering:

Perform K-Means clustering on the PCA-reduced data.
Perform DBSCAN clustering on the PCA-reduced data.
Evaluation:

Evaluate the clustering performance using silhouette scores and Davies-Bouldin scores.
Visualization:

Plot the clustering results for both PCA and t-SNE reduced data.
